{
  "name": "@bankrsynth/terminal",
  "version": "1.0.0",
  "description": "BankrSynth realtime terminal engine — SSE broadcaster + WebSocket server",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --dts --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@bankrsynth/shared": "workspace:*",
    "socket.io": "^4.7.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
